apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-camel.fullname" . }}
  labels:
    {{- include "sample-camel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "sample-camel.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
