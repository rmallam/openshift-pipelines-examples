apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Pipeline infrastructure only — PipelineRuns are ephemeral and excluded from GitOps sync.
resources:
  - universal-build-pipeline.yaml
  - universal-deploy-pipeline.yaml
  - rbac/pipeline-runner.yaml
  - pvc/pipeline-workspace-pvc.yaml
  - triggers/
