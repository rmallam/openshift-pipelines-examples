# Bootstrap Application — apply once to hand control to Argo CD.
#
#   oc apply -f argocd/root-application.yaml
#
# This App-of-Apps syncs the AppProject, tekton-pipelines Application, and
# sample-camel ApplicationSet from Git. After bootstrap, manage these via Git only.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tekton-root
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: tekton-pipelines
spec:
  project: default
  source:
    repoURL: https://github.com/rmallam/openshift-pipelines-examples.git
    targetRevision: main
    path: argocd
    directory:
      recurse: true
      include: '{app-project/*,applications/*,applicationsets/*}'
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
